# 🚀 Пошаговая инструкция по установке и запуску

## Шаг 1: Установка Node.js (если не установлен)

Node.js нужен для запуска проекта. Это бесплатно и безопасно.

### Вариант А: Скачать с официального сайта (рекомендуется)

1. Откройте сайт: **https://nodejs.org/**
2. Скачайте **LTS версию** (долгосрочная поддержка, стабильная)
   - Для Windows: нажмите кнопку "Download Node.js (LTS)"
   - Файл будет называться примерно `node-v20.x.x-x64.msi`
3. Запустите скачанный файл `.msi`
4. Следуйте инструкциям установщика (просто нажимайте "Next")
5. Перезапустите компьютер (или закройте и откройте терминал заново)

### Вариант Б: Через Chocolatey (если установлен)

```powershell
choco install nodejs-lts
```

### Проверка установки

Откройте **PowerShell** или **Командную строку** (cmd) и выполните:

```bash
node --version
npm --version
```

Должны появиться номера версий (например, `v20.11.0` и `10.2.4`).

Если команды не работают:
- Убедитесь, что перезапустили терминал после установки
- Проверьте, что Node.js установлен: Пуск → Node.js → Node.js command prompt

---

## Шаг 2: Открытие проекта в терминале

1. Откройте **PowerShell** или **Командную строку**
2. Перейдите в папку проекта:

```bash
cd G:\PROJECT
```

Или если вы в другой папке:
```bash
cd путь\к\вашему\проекту
```

Проверьте, что вы в правильной папке:
```bash
dir
```

Должны увидеть файлы: `package.json`, `index.html`, папку `src` и т.д.

---

## Шаг 3: Установка зависимостей проекта

Выполните команду (это займёт 1-2 минуты):

```bash
npm install
```

Что происходит:
- Скачиваются необходимые библиотеки (Vite, TypeScript)
- Создаётся папка `node_modules` (это нормально, её не нужно трогать)

Если видите ошибки:
- Убедитесь, что интернет подключен
- Попробуйте: `npm install --verbose` (покажет подробности)

---

## Шаг 4: Настройка Gemini API ключа

1. Откройте файл `src/config.ts` в любом текстовом редакторе
2. Найдите строку:
   ```typescript
   GEMINI_API_KEY: 'YOUR_GEMINI_API_KEY_HERE',
   ```
3. Замените `YOUR_GEMINI_API_KEY_HERE` на ваш реальный ключ

### Как получить ключ:

1. Откройте: **https://makersuite.google.com/app/apikey**
2. Войдите в Google аккаунт
3. Нажмите "Create API Key" или "Get API Key"
4. Скопируйте ключ (выглядит как длинная строка букв и цифр)
5. Вставьте в `src/config.ts` между кавычками

**Пример:**
```typescript
GEMINI_API_KEY: 'AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567',
```

Сохраните файл (Ctrl+S).

---

## Шаг 5: Запуск проекта

Выполните команду:

```bash
npm start
```

Что произойдёт:
- Запустится локальный сервер
- Автоматически откроется браузер на `http://localhost:5173`
- Если браузер не открылся сам, откройте вручную и перейдите по адресу

### Если команда `npm start` не работает:

Попробуйте альтернативную команду:
```bash
npm run dev
```

---

## ✅ Готово! Проект запущен

Вы должны увидеть:
- Главное меню с заголовком "Paper Galgame"
- Кнопки: Start, Partner, Settings, Community
- Изображение персонажа справа (если добавили спрайты)

---

## 🎮 Как использовать

1. Нажмите кнопку **Start**
2. Загрузите текстовый файл (.txt)
3. Нажмите **START LECTURE**
4. Дождитесь генерации истории (10-30 секунд)
5. Наслаждайтесь игрой!

---

## 🛑 Остановка проекта

В терминале нажмите:
```
Ctrl + C
```

Или просто закройте окно терминала.

---

## ❌ Решение проблем

### Ошибка: "npm не является внутренней или внешней командой"

**Решение:** Node.js не установлен или не добавлен в PATH
- Переустановите Node.js с официального сайта
- При установке убедитесь, что стоит галочка "Add to PATH"

### Ошибка: "Cannot find module"

**Решение:** Зависимости не установлены
```bash
npm install
```

### Ошибка: "Port 5173 is already in use"

**Решение:** Порт занят другим приложением
- Закройте другие запущенные проекты
- Или измените порт в `vite.config.ts`:
  ```typescript
  server: {
    port: 5174, // Измените на другой порт
  }
  ```

### Браузер открывается, но показывает ошибку

**Проверьте:**
1. Установлен ли Gemini API ключ в `src/config.ts`
2. Есть ли интернет-соединение
3. Откройте консоль браузера (F12) и посмотрите ошибки

### Проект не запускается после изменений

**Решение:**
```bash
# Остановите проект (Ctrl+C)
# Удалите папку node_modules
rmdir /s node_modules

# Переустановите зависимости
npm install

# Запустите снова
npm start
```

---

## 📝 Полезные команды

```bash
npm start          # Запустить проект (dev server)
npm run build      # Собрать проект для продакшена
npm run preview    # Просмотреть собранную версию
```

---

## 💡 Советы

1. **Не закрывайте терминал** пока проект запущен - это остановит сервер
2. **Изменения в коде** применяются автоматически (hot reload)
3. **Консоль браузера (F12)** поможет увидеть ошибки
4. **Первый запуск** может быть медленнее из-за установки зависимостей

---

## 🎯 Минимальные требования

- **Node.js:** версия 18 или выше
- **npm:** версия 9 или выше (устанавливается вместе с Node.js)
- **Интернет:** для работы с Gemini API
- **Браузер:** Chrome, Firefox, Edge (современные версии)

---

Удачи! Если что-то не работает - проверьте консоль браузера (F12) и терминал на ошибки.
