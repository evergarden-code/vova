# 📦 Инструкция по загрузке на GitHub и деплою на Render

## 🎯 Быстрый старт

### 1. Подготовка Git репозитория

```bash
# Перейдите в папку проекта
cd G:\PROJECT2_vova

# Инициализируйте git (если ещё не сделано)
git init

# Проверьте статус
git status
```

### 2. Проверьте .gitignore

Убедитесь, что `.gitignore` содержит:
```
.env
.env.local
.env.*.local
node_modules/
dist/
```

**Важно:** `.env` файл НЕ должен попасть в репозиторий!

### 3. Добавьте файлы в Git

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Проверьте, что будет закоммичено (убедитесь, что .env НЕ в списке!)
git status

# Создайте коммит
git commit -m "Initial commit: AI Visual Novel with Render deployment"
```

### 4. Создайте репозиторий на GitHub

**Вариант A: Через веб-интерфейс GitHub**
1. Перейдите на https://github.com
2. Нажмите **"New repository"** (или "+" → "New repository")
3. Название: `ai-visual-novel` (или любое другое)
4. Описание: `AI-powered Visual Novel with Gemini API`
5. Выберите **Public** или **Private**
6. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
7. Нажмите **"Create repository"**

**Вариант B: Через GitHub CLI**
```bash
# Установите GitHub CLI если ещё не установлен
# Затем:
gh repo create ai-visual-novel --public --source=. --remote=origin --push
```

### 5. Подключите remote и запушьте код

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/ai-visual-novel.git

# Или если используете SSH:
git remote add origin git@github.com:YOUR_USERNAME/ai-visual-novel.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Запушьте код
git push -u origin main
```

### 6. Проверьте на GitHub

1. Откройте ваш репозиторий на GitHub
2. Убедитесь, что все файлы загружены
3. Убедитесь, что `.env` файла НЕТ в репозитории

## 🚀 Деплой на Render

### Шаг 1: Создайте аккаунт на Render

1. Перейдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Зарегистрируйтесь через GitHub (рекомендуется) или email

### Шаг 2: Создайте новый Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий:
   - Если вы зарегистрировались через GitHub, выберите репозиторий из списка
   - Или нажмите **"Connect account"** и авторизуйтесь

### Шаг 3: Настройте сервис

**Основные настройки:**
- **Name:** `ai-visual-novel` (или любое другое имя)
- **Region:** Выберите ближайший регион (например, Frankfurt)
- **Branch:** `main` (или ваша основная ветка)
- **Root Directory:** оставьте пустым (корень проекта)
- **Runtime:** `Node`
- **Build Command:** `npm install && npm run build`
- **Start Command:** `npm start`

**Environment Variables (переменные окружения):**
Нажмите **"Add Environment Variable"** и добавьте:

| Key | Value |
|-----|-------|
| `GEMINI_API_KEY` | Ваш ключ от Gemini API (получите на https://aistudio.google.com/app/apikey) |
| `PORT` | `10000` |

**Важно:** 
- `GEMINI_API_KEY` - это ваш ключ от Google Gemini API
- Ключ должен быть без пробелов и кавычек

### Шаг 4: Деплой

1. Нажмите **"Create Web Service"**
2. Render начнёт сборку и деплой (обычно 2-5 минут)
3. Следите за логами в реальном времени
4. После успешного деплоя вы получите URL вида: `https://ai-visual-novel.onrender.com`

### Шаг 5: Проверка

1. Откройте ваш URL в браузере
2. Проверьте health check: `https://your-app.onrender.com/health`
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`
3. Загрузите файл и начните игру
4. Проверьте, что диалог генерируется корректно

## 🔄 Обновление кода

После внесения изменений:

```bash
# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Запушьте на GitHub
git push

# Render автоматически начнёт новый деплой!
```

Render автоматически обнаружит изменения и начнёт новый деплой.

## 🐛 Troubleshooting

### Ошибка при push: "Permission denied"
```bash
# Проверьте, что вы авторизованы в Git
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# Или используйте SSH ключи вместо HTTPS
```

### Ошибка: "GEMINI_API_KEY не установлен"
- Убедитесь, что переменная `GEMINI_API_KEY` добавлена в Render Dashboard → Environment Variables
- Перезапустите сервис после добавления переменной (Settings → Manual Deploy → Clear build cache & deploy)

### Ошибка при сборке: "Cannot find module"
- Проверьте, что все зависимости в `package.json`
- Убедитесь, что `npm install` выполняется в build command

### Сайт не загружается
- Проверьте логи в Render Dashboard → Logs
- Убедитесь, что `npm run build` выполнился успешно
- Проверьте health check: `/health`

## 📝 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1

# Посмотреть изменения
git diff

# Создать новую ветку
git checkout -b feature-name

# Переключиться на ветку
git checkout main
```

## ✅ Чеклист перед деплоем

- [ ] `.env` файл добавлен в `.gitignore`
- [ ] `.env` файл НЕ в репозитории (проверьте `git status`)
- [ ] Все зависимости в `package.json`
- [ ] `GEMINI_API_KEY` добавлен в Render Environment Variables
- [ ] Build command: `npm install && npm run build`
- [ ] Start command: `npm start`
- [ ] Health check path: `/health`

## 🎉 Готово!

После успешного деплоя ваш AI Visual Novel будет доступен по URL от Render. Поделитесь ссылкой с друзьями! 🚀
