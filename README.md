# 🎮 AI Visual Novel - Интерактивная новелла с Вовой

Визуальная новелла с AI-персонажем Вова, который живёт с матерью в Днепре на Максима Дия 8. Проект использует Google Gemini 2.5 Flash Lite для генерации динамических диалогов. Вы приходите к Вове домой, и дальше диалог развивается естественно: общение, приготовление еды, игры в New World, курение на балконе.

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Архитектура проекта](#-архитектура-проекта)
- [Система диалогов](#-система-диалогов)
- [Механики игры](#-механики-игры)
- [Локации и позы](#-локации-и-позы)
- [Интерфейс](#-интерфейс)
- [Настройка персонажа](#-настройка-персонажа)
- [Деплой на Render](#-деплой-на-render)

---

## 🚀 Быстрый старт

### Требования

- Node.js 18+ и npm
- API ключ Google Gemini (получить на [Google AI Studio](https://makersuite.google.com/app/apikey))

### Установка

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd PROJECT2_vova

# Установить зависимости
npm install

# Создать файл .env
echo "VITE_GEMINI_API_KEY=ваш_ключ_здесь" > .env

# Запустить dev сервер
npm start
```

Проект откроется на `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

---

## 🏗️ Архитектура проекта

### Структура файлов

```
PROJECT2_vova/
├── index.html              # Главная HTML страница
├── style.css              # Стили интерфейса
├── package.json           # NPM зависимости
├── vite.config.ts         # Конфигурация Vite
├── tsconfig.json          # Конфигурация TypeScript
│
├── src/
│   ├── script.ts          # Основная логика игры (UI, события, состояние)
│   ├── config.ts          # Конфигурация (позы, локации, музыка, настройки)
│   └── gemini-api.ts      # Интеграция с Gemini API (генерация диалогов)
│
├── assets/
│   ├── backgrounds/       # Фоны локаций (PNG, JPG, WebP)
│   │   ├── entrance.png   # Прихожая
│   │   ├── kitchen.png    # Кухня
│   │   ├── room.png       # Комната Вовы
│   │   └── balcony.png    # Балкон
│   ├── characters/        # Спрайты Вовы (PNG) - 7 файлов
│   └── music/             # Музыкальные темы (MP3)
│
└── README.md              # Этот файл
```

### Основные компоненты

#### 1. **`src/config.ts`** - Конфигурация
- Маппинг поз на файлы спрайтов (7 поз Вовы)
- Маппинг локаций (entrance, kitchen, room, balcony)
- Маппинг музыки
- Настройки по умолчанию
- API ключ Gemini

#### 2. **`src/gemini-api.ts`** - AI интеграция
- Класс `GeminiAPI` для работы с Gemini API
- Метод `generateStory()` - генерация диалогов
- Системный промпт (`VOVA_SYSTEM_PROMPT`) - определяет характер Вовы
- Парсинг JSON ответов от AI
- Логирование всех запросов/ответов

#### 3. **`src/script.ts`** - Логика игры
- Управление UI (диалоги, выборы, меню)
- Обработка файлов (текст/изображения)
- Анимации текста и спрайтов
- Управление музыкой и локациями
- Состояние игры (stage, mood, location, pose, action)

---

## 💬 Система диалогов

### Динамический диалог

**Вова сам решает**, когда дать слово игроку:
- Может говорить 2-3 реплики подряд, потом дать выбор
- Может говорить 5-10 реплик, если увлёкся темой (например, New World)
- Может дать слово игроку сразу после 1 реплики, если задаёт вопрос

**ГЛАВНОЕ:** Диалог должен быть живым и естественным, как в реальной жизни!

### Трехэтапная структура

Диалог проходит через три этапа:

#### **START** (Начало)
- Игрок загружает файл (текст или изображение) или начинает диалог без файла
- AI создаёт начальный диалог
- Вова приветствует игрока, предлагает что-то сделать
- Генерируется `core_summary` - краткое описание разговора
- Устанавливается начальный `mood_level` (50 - нейтральное)

#### **MIDDLE** (Развитие)
- Игрок делает выбор из 3 вариантов или вводит свой ответ
- AI продолжает диалог, реагируя на выбор
- Вова может менять локацию и позу в зависимости от диалога
- Может менять настроение в зависимости от выборов
- AI может сам решить перейти в FINAL или остаться в MIDDLE

#### **FINAL** (Завершение)
- AI завершает диалог логично
- Подводит итоги, делает выводы
- Вова прощается с игроком
- Показывается экран "The End"

### Формат ответа от AI

AI возвращает JSON в следующем формате:

```json
{
  "session_info": {
    "stage": "MIDDLE",
    "core_summary": "Краткое описание разговора...",
    "mood_level": 55,
    "location": "kitchen",
    "character_pose": "standing",
    "action": "cooking",
    "planned_events": [
      {
        "id": "cook_soup",
        "priority": "medium",
        "completed": false
      }
    ]
  },
  "frames": [
    {
      "id": 1,
      "text": "Ээ, короче, щас суп варю",
      "emotion": "NEUTRAL",
      "action": "*помешивает суп*"
    }
  ],
  "choices": [
    {
      "text": "Помочь готовить?",
      "mood_impact": 10
    },
    {
      "text": "А бананы сегодня фармил?",
      "mood_impact": -5
    },
    {
      "text": "Что нового?",
      "mood_impact": 0
    }
  ]
}
```

---

## 🎮 Механики игры

### Система настроения (Mood System)

**Начальное значение:** `mood_level = 50` (нейтральное)

**Влияние выборов:**
- Положительные выборы: `mood_impact: +5 до +15`
- Нейтральные выборы: `mood_impact: 0 до +5`
- Отрицательные выборы: `mood_impact: -5 до -20`

**Пороги:**
- `mood_level >= 80` - Вова весёлый, болтливый
- `mood_level >= 50` - Нормальное настроение
- `mood_level < 30` - Вова расстроен
- `mood_level < 20` - Вова угрюмый, может попросить игрока уйти

**Влияние на диалог:**
- AI получает описание настроения в промпте
- Вова реагирует на выборы согласно настроению
- Низкое настроение может привести к досрочному завершению

### Система выборов

**3 варианта от AI:**
- Разные по тону: дружелюбный, шутливый, провокационный
- Разный `mood_impact` в зависимости от тона

**4-й вариант - кастомный ввод:**
- Игрок может ввести свой текст
- AI анализирует текст на грубость/негативность
- Может снизить `mood_level` на 10-20 пунктов
- При повторной грубости - может завершить диалог

### Система локаций и поз

Вова может менять локацию и позу в зависимости от диалога:

**Локации:**
- `entrance` - прихожая (игрок только пришёл)
- `kitchen` - кухня (обычная или готовишь еду)
- `room` - комната Вовы (обычная или играешь в New World)
- `balcony` - балкон (обычный или куришь)

**Позы (для обычных сцен):**
- `standing` - стоишь (дефолт)
- `sitting_bed` - сидишь на кровати
- `thinking` - задумался, почёсываешь затылок
- `happy` - радостный
- `sad` - грустный
- `nervous` - нервный
- `annoyed` - раздражённый

**События (action) - для особых сцен:**
- `cooking` - готовишь еду на кухне (единая картинка: фон + Вова вместе)
- `gaming` - играешь в New World (единая картинка: фон + Вова за компом)
- `smoking` - куришь на балконе (единая картинка: фон + Вова курит)
- `null` - обычная сцена (фон + спрайт отдельно)

**ВАЖНО! Логика локаций и событий:**

1. **Обычная сцена** (диалог, обсуждение):
   - `location: "kitchen"`, `character_pose: "standing"`, `action: null`
   - Используется фон `kitchen.png` + спрайт `vova_standing.png` отдельно

2. **Событийная сцена** (готовка, игра, курение):
   - `location: "kitchen"`, `character_pose: "standing"`, `action: "cooking"`
   - Используется единая картинка `kitchen_cooking_vova.png` (фон + Вова вместе)

### Опциональная система событий

Вова может планировать события, которые **хотел бы** сделать, но **НЕ обязан** им следовать:

**Возможные события:**
- `cook_soup` - приготовить суп на кухне
- `play_new_world` - поиграть в New World, показать фарм бананов
- `smoke_balcony` - покурить на балконе, поговорить о жизни
- `watch_video` - посмотреть видео на ютубе
- `talk_war` - обсудить войну, ТЦК (если игрок спросит)
- `talk_escape` - обсудить план сбежать за границу

**Правила:**
1. В START этапе может запланировать 2-4 события
2. Если игрок выбирает что-то другое → **адаптируйся**, забей на план
3. Если игрок хочет уйти → **не заставляй**, пусть уходит (stage="FINAL")
4. События помогают структурировать диалог, но **не загоняют в рамки**

---

## 📍 Локации и позы

### Локации (фоны)

Все фоны находятся в `assets/backgrounds/`:

- **`entrance.png`** - Прихожая (игрок только пришёл)
- **`kitchen.png`** - Кухня (обычная сцена диалога)
- **`room.png`** - Комната Вовы (обычная сцена диалога)
- **`balcony.png`** - Балкон (обычная сцена диалога)

**Для событийных сцен используются единые картинки:**
- **`kitchen_cooking_vova.png`** - Кухня + Вова готовит (событийная)
- **`room_gaming_vova.png`** - Комната + Вова играет в New World (событийная)
- **`balcony_smoking_vova.png`** - Балкон + Вова курит (событийная)

### Позы (спрайты Вовы)

Всего **7 спрайтов** находятся в `assets/characters/`:

1. **`vova_standing.png`** - Стоит (дефолт, нейтральный)
2. **`vova_sitting_bed.png`** - Сидит на кровати
3. **`vova_thinking.png`** - Задумался, почёсывает затылок
4. **`vova_happy.png`** - Радостный (высокий mood, обсуждает New World)
5. **`vova_sad.png`** - Грустный (низкий mood)
6. **`vova_nervous.png`** - Нервный (обсуждает войну, ТЦК)
7. **`vova_annoyed.png`** - Раздражённый (шутят про маму, адрес)

**Требования к спрайтам:**
- Формат: PNG с прозрачным фоном (альфа-канал)
- Разрешение: минимум 1200x1600px (вертикальный формат)
- Позиция: персонаж по центру, ноги внизу кадра
- Стиль: реалистичный или полуреалистичный, визуальная новелла

---

## 🖥️ Интерфейс

### Главное меню

**Кнопки:**
- **Старт** - открывает загрузку файла (или начало диалога)
- **Партнёр** - "в разработке" (для будущих функций)
- **Настройки** - открывает панель настроек

**Элементы:**
- Фон меню (`menu_background.png`)
- Спрайт Вовы справа (`vova_standing.png`)
- Заголовок "Vova Visual Novel"

### Игровой экран

**Диалоговое окно:**
- Имя персонажа: "Вова" с иконкой ⭐
- Текст диалога (анимированный, посимвольный)
- Индикатор продолжения "▼"

**Кнопки выбора:**
- 3 варианта от AI (слева направо)
- 4-й вариант - кастомный ввод (текстовое поле)
- Позиционирование: справа, снизу от диалога

**Системные иконки (правый верхний угол):**
- ⚙️ **Настройки** - открывает панель настроек
- 🔊 **Громкость** - ползунок громкости (0-100)
- 🔄 **Сброс** - сбрасывает диалог, очищает состояние
- ↻ **Перезапуск** - перезагружает страницу
- ✕ **Выход** - выход с подтверждением

**Игровые кнопки (левый нижний угол):**
- **Auto** - автоматическое проигрывание (2 сек задержка)
- **Log** - история всех диалогов
- **Hide** - скрыть/показать UI

### Панель настроек

**Параметры:**
- **Скорость текста:** 10-100 (ползунок)
- **Громкость музыки:** 0-100 (ползунок)
- **Локация:** выбор из доступных (entrance, kitchen, room, balcony)
- **Музыкальная тема:** main_music_1 или main_music_2

**Синхронизация:**
- Ползунок громкости в настройках синхронизирован с ползунком в system-icons
- Иконка громкости меняется: 🔇 (0), 🔉 (<50), 🔊 (≥50)

### Экран загрузки

**Показывается при:**
- Генерации начального диалога (START)
- Генерации продолжения (MIDDLE/FINAL)

**Элементы:**
- Спиннер загрузки
- Заголовок: "Вова размышляет..."
- Подзаголовок: меняется динамически ("Вова готовится к разговору...", "Хм... как же начать...")
- Прогресс-бар (визуальный)

---

## 👤 Настройка персонажа

### Характер Вовы

Определён в `VOVA_SYSTEM_PROMPT` в `src/gemini-api.ts`:

- **Возраст:** 28 лет
- **Место:** Живёт с матерью в Днепре на Максима Дия 8
- **Работа:** Копирайтер на Qcomment (мало зарабатывает)
- **Хобби:** Фармит бананы в New World и продаёт их на бирже
- **Характер:** Ленивый, наивный, жизнерадостный и одновременно грустный, боится ТЦК
- **Стиль речи:** Простой язык с паразитами ("короче", "типа", "ну вот")

### Позы и спрайты

**Всего 7 поз** (в `src/config.ts`):

```typescript
POSES: {
  'standing': 'vova_standing.png',      // Стоит (дефолт)
  'sitting_bed': 'vova_sitting_bed.png', // Сидит на кровати
  'thinking': 'vova_thinking.png',      // Задумался
  'happy': 'vova_happy.png',            // Радостный
  'sad': 'vova_sad.png',                // Грустный
  'nervous': 'vova_nervous.png',        // Нервный
  'annoyed': 'vova_annoyed.png'         // Раздражённый
}
```

**Как добавить новую позу:**
1. Добавьте спрайт в `assets/characters/` (например, `vova_excited.png`)
2. Добавьте в `CONFIG.POSES` в `src/config.ts`:
   ```typescript
   'excited': 'vova_excited.png'
   ```
3. AI сможет использовать эту позу в диалогах

---

## 🚀 Деплой на Render

### Подготовка

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

2. **Проверьте `.gitignore`:**
   - Убедитесь, что `.env` в `.gitignore`
   - `node_modules/` и `dist/` должны быть игнорированы

### Настройка на Render

1. **Создайте новый Web Service на Render:**
   - Перейдите на [render.com](https://render.com)
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

2. **Настройки деплоя:**
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npx serve -s dist -l 10000`
   - **Environment:** `Node`

3. **Добавьте переменные окружения:**
   - В разделе "Environment" добавьте:
     - `VITE_GEMINI_API_KEY` = ваш API ключ

4. **Дополнительные настройки:**
   - **Root Directory:** оставьте пустым
   - **Auto-Deploy:** Yes (автоматический деплой при push)

### Альтернатива: статический хостинг

Если Render не подходит, можно использовать:

- **Vercel:** `vercel --prod`
- **Netlify:** подключите репозиторий, build command: `npm run build`, publish directory: `dist`
- **GitHub Pages:** используйте GitHub Actions для автоматического деплоя

### Проверка после деплоя

1. Откройте ваш сайт
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что API ключ загружается (не показывается alert об ошибке)
4. Протестируйте загрузку файла и генерацию диалога

---

## 📚 Дополнительная документация

- **`ASSETS_GUIDE.md`** - Подробное руководство по ассетам (7 спрайтов Вовы, локации)
- **`QUICKSTART.md`** - Быстрый старт
- **`ENV_SETUP.md`** - Настройка переменных окружения
- **`DEPLOY.md`** - Детальная инструкция по деплою

---

## 🛠️ Технологии

- **TypeScript** - типобезопасность и современный JS
- **Vite** - быстрый dev server и сборщик
- **Google Gemini 2.5 Flash Lite** - генерация диалогов через AI
- **CSS3** - анимации, градиенты, переходы
- **HTML5 Audio** - фоновая музыка
- **REST API** - прямое обращение к Gemini API (без SDK в браузере)

---

## 📝 Лицензия

Этот проект создан для образовательных целей. Используйте на свой риск.

---

## 🤝 Поддержка

Если у вас возникли проблемы:

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что API ключ установлен корректно
3. Проверьте, что все ассеты загружены с правильными именами
4. Проверьте логи Gemini API: `getGeminiLogs()` в консоли браузера

---

## 🎨 Генерация ассетов

Для генерации спрайтов и фонов можно использовать:
- **Stable Diffusion** - локальная генерация
- **Midjourney** - платная подписка
- **DALL-E** - через OpenAI API
- **Leonardo.ai** - бесплатный вариант

Подробные промпты для генерации находятся в README файлах каждой папки с ассетами.