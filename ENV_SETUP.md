# 🔐 Настройка переменных окружения (.env)

## Зачем нужен .env файл?

Файл `.env` используется для хранения секретных данных (API ключей), которые не должны попадать в репозиторий. Это безопасный способ хранения конфиденциальной информации.

## 📝 Как создать .env файл

### Шаг 1: Создайте файл `.env` в корне проекта

В корне проекта (там же, где `package.json`) создайте файл с именем `.env` (с точкой в начале!)

### Шаг 2: Добавьте API ключ

Откройте файл `.env` и добавьте:

```env
VITE_GEMINI_API_KEY=ваш_ключ_здесь
```

**Важно:**
- Используйте префикс `VITE_` (это требование Vite для клиентских переменных)
- Не ставьте кавычки вокруг ключа
- Не добавляйте пробелы вокруг `=`

### Пример:

```env
VITE_GEMINI_API_KEY=AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567
```

## 🔑 Как получить API ключ

1. Откройте: **https://makersuite.google.com/app/apikey**
2. Войдите в Google аккаунт
3. Нажмите **"Create API Key"** или **"Get API Key"**
4. Скопируйте ключ (длинная строка букв и цифр)
5. Вставьте в `.env` файл

## ✅ Проверка

После создания `.env` файла:

1. Перезапустите dev server (`npm start`)
2. Откройте консоль браузера (F12)
3. Если ключ установлен правильно - предупреждений не будет
4. Если ключ не установлен - увидите ошибку

## 🚫 Безопасность

### Что уже защищено:

- ✅ Файл `.env` уже в `.gitignore` - не попадёт в Git
- ✅ Файл `.env.local` тоже игнорируется
- ✅ При коммите ключ не будет загружен в репозиторий

### Для хостинга (Render, Vercel, Netlify и т.д.):

**Настройте переменные окружения в панели хостинга:**

1. Зайдите в настройки проекта на вашем хостинге
2. Найдите раздел "Environment Variables" или "Переменные окружения"
3. Добавьте:
   - **Имя:** `VITE_GEMINI_API_KEY`
   - **Значение:** ваш API ключ
4. Сохраните и перезапустите проект

**Пример для Render:**
```
Environment Variables:
  VITE_GEMINI_API_KEY = AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567
```

## 📁 Структура файлов

```
/project
├── .env              ← Ваш файл с ключом (НЕ коммитится в Git)
├── .env.example      ← Шаблон (можно коммитить)
├── .gitignore        ← Уже содержит .env
└── src/
    └── config.ts     ← Читает из import.meta.env.VITE_GEMINI_API_KEY
```

## ⚠️ Важные замечания

1. **Никогда не коммитьте `.env` файл** - он уже в `.gitignore`
2. **Не публикуйте ключ** в открытом доступе
3. **Используйте разные ключи** для разработки и продакшена
4. **Префикс `VITE_` обязателен** - без него переменная не будет доступна в клиентском коде

## 🔄 После изменения .env

После изменения `.env` файла нужно **перезапустить dev server**:

```bash
# Остановите сервер (Ctrl+C)
# Запустите снова
npm start
```

Vite автоматически загрузит новые переменные окружения.

## 🐛 Решение проблем

### Ключ не работает

1. Проверьте, что файл называется именно `.env` (с точкой в начале)
2. Проверьте, что используется префикс `VITE_`
3. Перезапустите dev server
4. Проверьте консоль браузера на ошибки

### Переменная не доступна

- Убедитесь, что используется `import.meta.env.VITE_GEMINI_API_KEY`
- Проверьте, что сервер перезапущен после изменения `.env`
- В продакшене убедитесь, что переменная установлена в настройках хостинга
