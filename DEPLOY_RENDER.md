# 🚀 Деплой на Render

Пошаговая инструкция по деплою AI Visual Novel на Render.

## 📋 Подготовка

### 1. Получите Gemini API ключ

1. Перейдите на https://aistudio.google.com/app/apikey
2. Войдите в свой Google аккаунт
3. Нажмите "Create API Key"
4. Скопируйте ключ (он понадобится позже)

### 2. Подготовьте репозиторий на GitHub

```bash
# Инициализируйте git (если ещё не сделано)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: AI Visual Novel ready for Render"

# Создайте репозиторий на GitHub (через веб-интерфейс или GitHub CLI)
# Затем добавьте remote и запушьте:
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

**Важно:** Убедитесь, что `.env` файл добавлен в `.gitignore` (уже есть)!

## 🌐 Деплой на Render

### Шаг 1: Создайте аккаунт на Render

1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub (рекомендуется) или email

### Шаг 2: Создайте новый Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий:
   - Выберите репозиторий из списка
   - Или подключите новый репозиторий

### Шаг 3: Настройте сервис

**Основные настройки:**
- **Name:** `ai-visual-novel` (или любое другое имя)
- **Region:** Выберите ближайший регион (например, Frankfurt)
- **Branch:** `main` (или ваша основная ветка)
- **Root Directory:** оставьте пустым (корень проекта)
- **Runtime:** `Node`
- **Build Command:** `npm install && npm run build`
- **Start Command:** `npm start`

**Environment Variables (переменные окружения):**
Нажмите **"Add Environment Variable"** и добавьте:

```
GEMINI_API_KEY = ваш_ключ_от_gemini
PORT = 10000
```

**Важно:** 
- `GEMINI_API_KEY` - это ваш ключ от Google Gemini API
- `PORT` - Render автоматически устанавливает переменную `PORT`, но мы указываем значение по умолчанию

### Шаг 4: Деплой

1. Нажмите **"Create Web Service"**
2. Render начнёт сборку и деплой
3. Дождитесь завершения (обычно 2-5 минут)
4. После успешного деплоя вы получите URL вида: `https://ai-visual-novel.onrender.com`

## ✅ Проверка работоспособности

1. Откройте ваш URL в браузере
2. Проверьте health check: `https://your-app.onrender.com/health`
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`
3. Загрузите файл и начните игру
4. Проверьте, что диалог генерируется корректно

## 🔧 Локальная разработка

Для локальной разработки:

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` и добавьте ваш Gemini API ключ:
   ```
   GEMINI_API_KEY=your_gemini_api_key_here
   PORT=10000
   ```

3. Установите зависимости:
   ```bash
   npm install
   ```

4. Запустите dev сервер (Vite):
   ```bash
   npm run dev
   ```

5. Или запустите production сервер:
   ```bash
   npm run build
   npm start
   ```

## 📝 Структура проекта

```
PROJECT2_vova/
├── server.js              # Backend сервер (Express)
├── src/
│   ├── gemini-api.ts      # Клиент для API (обращается к /api/generate-story)
│   └── script.ts          # Основная логика игры
├── render.yaml            # Конфигурация для Render (опционально)
├── .env.example          # Пример переменных окружения
├── .gitignore            # Игнорирует .env и другие файлы
└── package.json          # Зависимости и скрипты
```

## 🔒 Безопасность

- ✅ `.env` файл уже добавлен в `.gitignore`
- ✅ API ключ хранится только в переменных окружения Render
- ✅ Backend сервер проксирует запросы к Gemini API (решает CORS)
- ✅ Ключ никогда не попадает в браузер клиента

## 🐛 Troubleshooting

### Ошибка: "GEMINI_API_KEY не установлен"
- Убедитесь, что переменная `GEMINI_API_KEY` добавлена в Render Dashboard → Environment Variables
- Перезапустите сервис после добавления переменной

### Ошибка: "CORS error"
- Убедитесь, что используете backend сервер (`npm start`), а не Vite dev server
- Проверьте, что `server.js` запущен и доступен на `/api/generate-story`

### Ошибка: "Cannot find module 'express'"
- Выполните `npm install` перед запуском
- Убедитесь, что все зависимости установлены

### Сайт не загружается
- Проверьте логи в Render Dashboard → Logs
- Убедитесь, что `npm run build` выполнился успешно
- Проверьте health check: `/health`

## 📚 Дополнительные ресурсы

- [Render Documentation](https://render.com/docs)
- [Gemini API Documentation](https://ai.google.dev/docs)
- [Express.js Documentation](https://expressjs.com/)

## 🎉 Готово!

После успешного деплоя ваш AI Visual Novel будет доступен по URL от Render. Поделитесь ссылкой с друзьями! 🚀
