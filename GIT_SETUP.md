# 📦 Настройка Git репозитория

## Первоначальная настройка

### 1. Инициализация репозитория

```bash
# Если репозиторий ещё не инициализирован
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: AI Visual Novel"
```

### 2. Подключение к GitHub

```bash
# Добавьте remote (замените на ваш URL)
git remote add origin https://github.com/yourusername/ai-visual-novel.git

# Отправьте код
git push -u origin main
```

**Если репозиторий уже существует на GitHub:**

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/ai-visual-novel.git
cd ai-visual-novel

# Скопируйте файлы проекта в эту папку
# Затем:
git add .
git commit -m "Initial commit"
git push -u origin main
```

## Важные файлы для Git

### ✅ Должны быть в репозитории:

- Все файлы в `src/`
- `index.html`, `style.css`
- `package.json`, `package-lock.json`
- `vite.config.ts`, `tsconfig.json`
- Все файлы в `assets/` (фоны, спрайты, музыка)
- `README.md`, `DEPLOY.md`, `render.yaml`
- `.gitignore`

### ❌ НЕ должны быть в репозитории:

- `.env` (содержит API ключ)
- `node_modules/` (устанавливается через `npm install`)
- `dist/` (генерируется при сборке)
- Временные файлы (см. `.gitignore`)

## Проверка перед коммитом

```bash
# Проверьте статус
git status

# Убедитесь, что .env не добавлен
git check-ignore .env
# Должно вывести: .env

# Проверьте, что node_modules игнорируется
git check-ignore node_modules/
# Должно вывести: node_modules/
```

## Рабочий процесс

### Обычные изменения:

```bash
# Внесите изменения в код
# ...

# Проверьте статус
git status

# Добавьте изменённые файлы
git add .

# Или добавьте конкретные файлы
git add src/script.ts index.html

# Создайте коммит
git commit -m "Описание изменений"

# Отправьте в репозиторий
git push origin main
```

### Создание новой ветки:

```bash
# Создайте ветку для новой функции
git checkout -b feature/new-character

# Внесите изменения
# ...

# Закоммитьте
git add .
git commit -m "Добавлен новый персонаж"

# Отправьте ветку
git push origin feature/new-character

# Вернитесь на main
git checkout main
```

## Полезные команды

```bash
# Просмотр истории коммитов
git log --oneline

# Просмотр изменений
git diff

# Отмена изменений в файле
git checkout -- filename

# Отмена последнего коммита (сохраняя изменения)
git reset --soft HEAD~1

# Просмотр удалённых репозиториев
git remote -v
```

## Настройка .gitignore

Файл `.gitignore` уже настроен и включает:

- `node_modules/` - зависимости
- `dist/` - собранные файлы
- `.env` - переменные окружения
- Логи и временные файлы
- Настройки IDE

**Не редактируйте `.gitignore` без необходимости!**

## Проблемы и решения

### Ошибка: "fatal: not a git repository"

**Решение:** Выполните `git init` в корне проекта

### Ошибка: "remote origin already exists"

**Решение:** 
```bash
# Удалите старый remote
git remote remove origin

# Добавьте новый
git remote add origin <your-url>
```

### Случайно добавили .env

**Решение:**
```bash
# Удалите из индекса (но оставьте файл)
git rm --cached .env

# Добавьте в .gitignore (если ещё не добавлен)
echo ".env" >> .gitignore

# Закоммитьте
git add .gitignore
git commit -m "Исключить .env из репозитория"
```

---

**Готово!** Ваш проект готов к работе с Git и GitHub.
