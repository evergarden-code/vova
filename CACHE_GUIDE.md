# 🚀 Руководство по кэшированию контекста (Context Caching)

## Что это такое?

Кэширование контекста позволяет оптимизировать работу с большими файлами. Вместо того чтобы каждый раз отправлять весь файл и системный промпт (которые могут занимать десятки тысяч токенов), мы создаём кэш один раз, а затем используем только его ID.

## Как это работает

### Без кэширования:
```
Каждый запрос = [Системный промпт + Весь файл + История + Новый выбор]
Стоимость: ~30,000+ токенов каждый раз
```

### С кэшированием:
```
Создание кэша (1 раз) = [Системный промпт + Весь файл]
Последующие запросы = [ID кэша + История + Новый выбор]
Стоимость: ~100-200 токенов каждый раз
```

## Требования

- **Минимальный размер:** Кэширование работает только для файлов > 32,768 токенов
- **Модель:** `gemini-1.5-flash-001` (для кэша)
- **TTL:** Кэш живет 1 час (3600 секунд)

## Как это реализовано в проекте

### 1. Оптимизированная архитектура

**Статическая часть (в кэше):**
- `System Instruction` - личность Лены, правила игры (через `systemInstruction`, не как сообщение)
- Весь файл пользователя (текст или изображение)

**Динамическая часть (в каждом запросе):**
- Только выбор игрока
- `core_summary` - краткая суть прошлых событий
- Последние 2 реплики истории (обрезанная)

### 2. Автоматическое создание кэша

При загрузке файла система автоматически пытается создать кэш:

```typescript
// В startGame()
const cacheName = await geminiAPI.createCache(fileData);
```

**Если кэш создан успешно:**
- Сохраняется ID кэша
- Последующие запросы используют кэш
- Экономия токенов и денег (~90% для больших файлов)

**Если кэш не создан (файл < 32k токенов):**
- Система автоматически переключается на обычный режим
- Использует `systemInstruction` вместо первого сообщения
- Обрезает историю до последних 2 реплик

### 3. Использование кэша

**Для START этапа:**
```typescript
if (cacheName) {
    currentStoryData = await geminiAPI.generateStoryWithCache('START');
} else {
    currentStoryData = await geminiAPI.generateStory(fileData, 'START');
}
```

**Для MIDDLE и FINAL этапов:**
```typescript
if (geminiAPI.hasCache()) {
    currentStoryData = await geminiAPI.generateStoryWithCache(
        nextStage,
        choice.text,
        coreSummary
    );
} else {
    // Обычный режим с systemInstruction
}
```

### 4. Оптимизации

**System Instructions:**
- `SYSTEM_PROMPT` передаётся через `systemInstruction`, а не как первое сообщение
- Экономия токенов и более эффективная обработка

**JSON Mode:**
- Все запросы используют `responseMimeType: "application/json"`
- Гарантирует чистый JSON без markdown обёрток
- Не нужны регулярные выражения для очистки
- Меньше токенов на ответ

**Обрезка истории:**
- Хранится только последние 2 реплики (4 элемента: user + model)
- Предотвращает раздувание контекста
- Используется `core_summary` для сохранения контекста

## Преимущества

### 💰 Экономия денег
- При файле в 50k токенов:
  - Без кэша: 50k токенов × 10 запросов = 500k токенов
  - С кэшем: 50k (кэш, платишь 1 раз) + 200 токенов × 10 = 52k токенов
  - **Экономия: ~90%**

### ⚡ Скорость
- Кэшированные запросы обрабатываются быстрее
- Модели не нужно "перечитывать" весь файл каждый раз
- JSON Mode ускоряет парсинг ответов

### 📊 Масштабируемость
- Можно работать с очень большими файлами
- История диалога не растёт бесконечно (обрезается до 2 реплик)
- `core_summary` сохраняет контекст без раздувания токенов

### 🎯 Оптимизация запросов
- System Instructions вместо сообщений - экономия токенов
- Короткие промпты (без упоминаний JSON, так как JSON Mode гарантирует формат)
- Обрезка истории предотвращает рост стоимости

## Технические детали

### Методы API

**`createCache(fileData)`**
- Создаёт кэш для файла
- Возвращает `cacheName` (ID кэша) или `null`
- Работает с текстом и изображениями

**`generateStoryWithCache(stage, playerChoice, coreSummary)`**
- Использует существующий кэш
- Отправляет только короткий контекст
- Возвращает `StoryData`

**`hasCache()`**
- Проверяет, есть ли активный кэш
- Возвращает `boolean`

**`clearCache()`**
- Очищает кэш
- Вызывается при отмене загрузки файла

### Обработка ошибок

- Если кэш не создан (файл слишком маленький) → автоматический fallback на обычный режим
- Если кэш истёк (TTL) → система попытается создать новый
- Все ошибки логируются в консоль

## Рекомендации

1. **Для больших файлов (> 32k токенов):**
   - Кэш создастся автоматически
   - Используйте его для экономии

2. **Для маленьких файлов (< 32k токенов):**
   - Кэш не создастся (это нормально)
   - Система работает в обычном режиме

3. **Оптимизация истории:**
   - Используйте `core_summary` для сохранения контекста
   - Не храните всю историю диалогов

4. **TTL кэша:**
   - По умолчанию: 1 час
   - Для длинных игр можно продлевать
   - Кэш автоматически удалится после TTL

## Мониторинг

В консоли браузера вы увидите:
- `"Кэш создан успешно: cachedContents/..."` - если кэш создан
- `"Кэш не создан (возможно, файл слишком маленький): ..."` - если файл < 32k токенов

## Итого

Система автоматически определяет, нужен ли кэш, и использует его когда возможно. Вам не нужно ничего настраивать вручную - всё работает автоматически! 🎉
