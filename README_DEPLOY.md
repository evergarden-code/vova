# 🚀 Быстрый деплой на Render

## 📋 Что нужно сделать

### 1. Подготовка кода на GitHub

```bash
# 1. Инициализируйте git (если ещё не сделано)
git init

# 2. Добавьте все файлы
git add .

# 3. Создайте коммит
git commit -m "Ready for Render deployment"

# 4. Создайте репозиторий на GitHub (через веб-интерфейс)
# 5. Подключите remote и запушьте
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

**Важно:** Убедитесь, что `.env` файл НЕ попал в репозиторий (он уже в `.gitignore`)!

### 2. Получите Gemini API ключ

1. Перейдите на https://aistudio.google.com/app/apikey
2. Войдите в Google аккаунт
3. Нажмите "Create API Key"
4. Скопируйте ключ (понадобится в следующем шаге)

### 3. Деплой на Render

1. **Создайте аккаунт:** https://render.com → Sign Up (через GitHub)
2. **Создайте Web Service:**
   - New + → Web Service
   - Подключите ваш GitHub репозиторий
3. **Настройки:**
   - **Name:** `ai-visual-novel`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
4. **Environment Variables:**
   - `GEMINI_API_KEY` = ваш ключ от Gemini
   - `PORT` = `10000`
5. **Создайте сервис** → Render автоматически начнёт деплой

### 4. Готово! 🎉

После деплоя ваш сайт будет доступен по URL вида:
`https://ai-visual-novel.onrender.com`

## 🔧 Локальная разработка

```bash
# 1. Установите зависимости
npm install

# 2. Создайте .env файл
echo "GEMINI_API_KEY=your_key_here" > .env
echo "PORT=10000" >> .env

# 3. Запустите dev сервер (Vite)
npm run dev

# ИЛИ запустите production сервер
npm run build
npm start
```

## 📁 Структура

- `server.js` - Backend сервер (Express) для проксирования запросов к Gemini API
- `src/gemini-api.ts` - Клиент для API (обращается к `/api/generate-story`)
- `src/script.ts` - Основная логика игры
- `render.yaml` - Конфигурация для Render (опционально)

## 🐛 Troubleshooting

**Ошибка: "GEMINI_API_KEY не установлен"**
- Добавьте переменную в Render Dashboard → Environment Variables
- Перезапустите сервис

**CORS ошибка**
- Убедитесь, что используете `npm start` (backend), а не `npm run dev` (Vite)

**Сайт не загружается**
- Проверьте логи в Render Dashboard
- Проверьте health check: `/health`

## 📚 Подробные инструкции

- **Полная инструкция по Git и деплою:** `GIT_DEPLOY.md`
- **Детальный гайд по Render:** `DEPLOY_RENDER.md`

---

**Удачи! 🚀**
