# 🎮 Руководство по главному меню

## ✅ Главное меню УЖЕ реализовано в коде!

Главное меню находится в файле `index.html` (строки 48-80) и стилизовано в `style.css`. Вам **НЕ нужно** создавать отдельные ассеты для меню!

---

## 📋 Что уже есть в коде:

### 1. **HTML структура** (`index.html`)
- ✅ Заголовок "Paper Galgame" с подзаголовком "オトメ・ドメイン"
- ✅ Кнопки: Start, Partner, Settings, Community & Author
- ✅ Секция загрузки файла (появляется при нажатии Start)
- ✅ Место для изображения персонажа справа

### 2. **CSS стили** (`style.css`)
- ✅ Розово-голубой градиентный фон (CSS, не файл!)
- ✅ Диагональные белые полоски (CSS паттерн)
- ✅ Стили кнопок (градиенты, анимации)
- ✅ Позиционирование элементов

### 3. **JavaScript логика** (`src/script.ts`)
- ✅ Обработчики кнопок меню
- ✅ Загрузка файла
- ✅ Переход к игровому экрану

---

## 🖼️ Какие ассеты нужны для меню:

### 1. Фон главного меню:

**`assets/backgrounds/menu_background.jpg`**

Фон классной комнаты для главного меню. Если файл не найден, будет показан градиентный фон (fallback).

### 2. Спрайт персонажа:

**`assets/characters/lena_neutral.png`**

Это тот же спрайт, который используется в игре! Меню автоматически показывает нейтральную эмоцию персонажа справа.

**Где используется:**
```typescript
// В src/script.ts, строка 123
menuCharImg.src = `/assets/characters/${CONFIG.EMOTIONS.NEUTRAL}`;
// Это загружает: /assets/characters/lena_neutral.png
```

---

## 🎨 Как выглядит главное меню:

```
┌─────────────────────────────────────────────────┐
│                                                 │
│         Paper Galgame                          │
│         オトメ・ドメイン                        │
│                                                 │
│                    [👤 Лена]  ← Справа         │
│                    (lena_neutral.png)           │
│                                                 │
│         [Start]      ← Кнопки слева            │
│         [Partner]                               │
│         [Settings]                              │
│         [Community & Author]                    │
│                                                 │
│  (При нажатии Start появляется:)               │
│  "Загрузите текстовый файл"                    │
│  [Выбрать файл...]                             │
│  [START LECTURE]                               │
│                                                 │
└─────────────────────────────────────────────────┘
```

**Фон:** Розово-голубой градиент (CSS, не файл!)
**Персонаж:** Справа, `lena_neutral.png`
**Кнопки:** Слева, стилизованы через CSS

---

## 📁 Структура файлов меню:

```
/project
├── index.html          ← Главное меню здесь (строки 48-80)
├── style.css           ← Стили меню здесь
└── assets/
    └── characters/
        └── lena_neutral.png  ← ЕДИНСТВЕННЫЙ ассет для меню!
```

---

## 🔍 Где найти код меню:

### HTML (`index.html`):
```html
<!-- Стартовый экран / Главное меню -->
<div id="start-screen" class="start-screen">
    <div class="menu-title">
        <h1 class="title-paper">Paper</h1>
        <h1 class="title-galgame">Galgame</h1>
        <p class="subtitle">オトメ・ドメイン</p>
    </div>
    <div class="menu-character">
        <img id="menu-character-img" src="" alt="Лена">
    </div>
    <div class="menu-buttons">
        <button id="start-menu-button">Start</button>
        <!-- и т.д. -->
    </div>
</div>
```

### CSS (`style.css`):
- `.start-screen` - фон меню (градиент)
- `.menu-title` - стили заголовка
- `.menu-buttons` - стили кнопок
- `.menu-character` - позиционирование персонажа

### JavaScript (`src/script.ts`):
- Строка 123: загрузка спрайта для меню
- Обработчики кнопок меню

---

## ✅ Итого для главного меню:

**Вам нужно:**
- ✅ `menu_background.jpg` в папке `assets/backgrounds/` - фон классной комнаты
- ✅ `lena_neutral.png` в папке `assets/characters/` - спрайт персонажа

**Вам НЕ нужно создавать:**
- ❌ Отдельные иконки кнопок (используется текст + CSS)
- ❌ Отдельный спрайт для меню (используется тот же, что и в игре)

---

## 🎯 Минимальный набор для работы меню:

1. **Один спрайт:** `assets/characters/lena_neutral.png`
2. Всё остальное уже в коде!

Меню будет работать даже без спрайта (просто не будет изображения персонажа справа).

---

## 💡 Как проверить меню:

1. Запустите проект: `npm start`
2. Откройте браузер: `http://localhost:5173`
3. Вы увидите главное меню с:
   - Заголовком "Paper Galgame"
   - Кнопками слева
   - Изображением Лены справа (если добавили `lena_neutral.png`)

---

## 🔧 Если меню не отображается:

1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что файл `index.html` загружается
3. Проверьте, что CSS файл подключен: `<link rel="stylesheet" href="style.css">`
4. Убедитесь, что JavaScript файл подключен: `<script type="module" src="/src/script.ts"></script>`

---

**Вывод:** Главное меню уже полностью готово в коде! Вам нужен только спрайт `lena_neutral.png`, который уже в списке необходимых ассетов. 🎉
