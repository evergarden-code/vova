# 🚀 Обновление до Gemini 2.5 Flash Lite

Проект обновлён для использования новой модели **Gemini 2.5 Flash Lite** с унифицированным SDK `@google/genai`.

## 📦 Установка зависимостей

```bash
npm install
```

Это установит официальный пакет `@google/generative-ai` (версия 0.21.0+).

## ✅ Правильный пакет

Проект использует **официальный SDK от Google**: `@google/generative-ai`

- **Пакет:** `@google/generative-ai`
- **Импорт:** `import { GoogleGenerativeAI } from '@google/generative-ai';`
- **Установка:** `npm install @google/generative-ai`

## 🎯 Что изменилось

### 1. Новая модель
- **Старая:** `gemini-1.5-flash`
- **Новая:** `gemini-2.5-flash-lite`

### 2. Официальный SDK
- Используется официальный SDK `@google/generative-ai`
- Стандартный синтаксис API от Google
- Поддержка `thinkingBudget` для управления режимом рассуждений

### 3. Параметр thinkingBudget

Новая модель поддерживает параметр `thinkingBudget` для управления режимом рассуждений:

- **`thinkingBudget: 0`** - режим рассуждений отключён (максимальная скорость)
- **`thinkingBudget: -1`** - динамический режим (модель сама решает)
- **`thinkingBudget: > 0`** - фиксированный бюджет токенов на рассуждения (512-24576)

**Текущая настройка:** `thinkingBudget: 0` (максимальная скорость)

**Для улучшения качества ответов** можно увеличить до:
```typescript
thinkingConfig: {
  thinkingBudget: 512  // или больше для сложных задач
}
```

## 🔧 Конфигурация

### config.ts
Модель обновлена в `src/config.ts`:
```typescript
GEMINI_MODEL: 'gemini-2.5-flash-lite',
```

### gemini-api.ts
Основные изменения:
- Использование нового SDK `GoogleGenAI`
- Поддержка `thinkingConfig` с `thinkingBudget`
- Сохранена вся функциональность (кэширование, JSON Mode, обработка изображений)

## 🚀 Запуск

После установки зависимостей:

```bash
npm start
```

## 📝 Примечания

1. **Совместимость:** Код обратно совместим - все существующие функции работают как прежде
2. **Кэширование:** Поддержка кэширования контекста сохранена для больших файлов
3. **JSON Mode:** Режим JSON остаётся активным для гарантированного формата ответов
4. **Браузерная среда:** Код адаптирован для работы в браузере через Vite

## 🐛 Решение проблем

### Ошибка: "Cannot find module '@google/generative-ai'"

**Решение:** Установите пакет:
```bash
npm install @google/generative-ai
```

Убедитесь, что в `package.json` есть зависимость:
```json
"dependencies": {
  "@google/generative-ai": "^0.21.0"
}
```

### Ошибка: "API key not found"

Убедитесь, что в `.env` файле установлен ключ:
```env
VITE_GEMINI_API_KEY=ваш_ключ
```

### Медленные ответы

Если ответы слишком медленные, убедитесь, что `thinkingBudget: 0` установлен в `src/gemini-api.ts`.

## 📚 Дополнительная информация

- [Документация Gemini 2.5 Flash Lite](https://ai.google.dev/gemini-api/docs)
- [Thinking Mode в Gemini 2.5](https://ai.google.dev/gemini-api/docs/thinking)
- [Google AI Studio](https://makersuite.google.com/app/apikey)
